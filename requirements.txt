# HYBRID-RAG — Python backend dependencies
# Install with: pip install -r requirements.txt

# FastAPI server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Request validation
pydantic>=2.0.0

# Retrieval stack (existing V2 pipeline)
numpy>=1.26.0
rank-bm25>=0.2.2
sentence-transformers>=3.0.0
tiktoken>=0.7.0
requests>=2.31.0

# Embeddings (query-side only; corpus vectors are pre-computed)
# langchain-community is NOT required at runtime; dense.py calls Jina directly

# Environment
python-dotenv>=1.0.0

# Generation
openai>=1.0.0      # for GPT-4o-mini
google-genai>=0.8.0  # for Gemini (fallback)

# Document Processing & Uploads
python-multipart>=0.0.12
pypdf>=4.0.0

